---
title: "DuckDB with Hannes Mühleisen (SE Daily 2022)"
date: 2022-03-18
event: "Software Engineering Daily"
video_url: "https://softwareengineeringdaily.com/2022/03/18/duckdb-with-hannes-muleisen/"
video_type: "Podcast"
transcribed: 2025-01-29
---

*This transcript was AI-generated using Whisper and may contain minor errors.*

## Transcript

DuckDB is a relational database management system with no external dependencies and a simple system for deployment and integration into build processes. It enables complex queries in SQL with a large function library and provides transactional guarantees through multi-version concurrency control. Hannes Mühleisen works on DuckDB and joins the show to talk about query engines and OLAP systems. If you're interested in sponsoring Software Engineering Daily, reach out to us at sponsor at softwareengineeringdaily.com. We'd love to hear from you and bring your message to our audience. We reach over 250,000 developers per month.

Hannes, welcome to the show. Thank you for having me. You are working on DuckDB, which is an approach to doing OLAP processing. Can you give an overview of the current state of the art, different options for OLAP query processing, and maybe give an overview for what OLAP is for people who don't know?

Sure. Happy to. So this comes a bit from the SQL world and there's this general misconception of people that kind of connect the language to specific systems, but there is a whole wide range of different implementations of the language, execution engines for the SQL language that exists out there. And there's like two main classes of systems that exist there. One is the OLTP, online transactional processing, and one is OLAP, online analytical processing. And these are very, very different in the kind of use case they're used for. So most people are familiar with OLTP because the systems that people are very familiar with like MySQL, Postgres, SQLite, traditional Oracle SQL Server, the whole list, they are all OLTP or at least they come from this world. And that's great if you're running like a web shop or something like that, because your queries will be focusing on individual rows. You're adding rows, you're deleting rows, you're updating rows, this kind of stuff.

But once you're trying to do analytics, like driving a dashboard, for example, creating reports, statistics, ML, what have you, these systems are not really ideal anymore. And people have been designing custom SQL systems called OLAP systems for this purpose. And I think these are less well-known, especially in the open source space. There's not a lot of them. Traditionally, I think for a long time, there was something called MonetDB, which was kind of the only representative there. There were some commercial systems. And these days, I think what people would be most familiar with is ClickHouse. Maybe you're aware of it. Sure. Yeah. So ClickHouse, I think, is at the moment the most well-known system there. And then, of course, you also have the distributed camp like Spark and Presto and all the other systems in that class. But I think this is an entirely different camp, the distributed Big Data SQL kind of systems. But I think ClickHouse would be the most well-known representative there. But we don't work on ClickHouse. We work on DuckDB, which is, I think, yet another take on this OLAP problem.

These different systems have trade-offs in terms of latency, cost, complexity, API. You are working on DuckDB. Can you give an overview of how DuckDB compares to these different OLAP systems and what the most important competitive differentiation is?

Yeah. Happy to. So I think the design goals for DuckDB came kind of out from talking to people, data scientists specifically, who were not using databases. And we tried to figure out why that was. And they gave us all sorts of reasons. For example, they said, oh, these databases are horrible to use. They're difficult to set up, they're difficult to maintain, they're difficult to get running in the first place. You have a lot of problems with data transfer in and out. So this kind of drove the design goals for DuckDB. We tried to differentiate by being super easy to install, which is why DuckDB is a 10 megabyte kind of binary that you install without anywhere really that you can just run. And I think the biggest difference is from the architecture level where traditional systems, I think without exceptions, are these days all client-server, where a database server sits somewhere and you have a client protocol, a client program somewhere, maybe as a library. And between those client libraries and the database server, you have some sort of client protocol.

DuckDB is very different there because it's a database as a library. We call it the in-process OLAP. And it's the first one because we kind of invented this as a class of systems, the in-process OLAP that just didn't exist before. It's kind of interesting. Sometimes we're wondering why nobody has done this, but there we are.

So when you say it's an in-process SQL OLAP database management system, what does in-process mean? That is super cool. You're aware of SQLite, right? Yes. Okay. So SQLite doesn't have a server in that sense. The way you interact with SQLite is you link that to your application like a shared library or you even compile it in. And then there is no separate process that runs your database. The database is just a set of API calls into the SQLite library. And DuckDB works the same way. You basically start up the database server within your application. For example, very often that application will be Python and then your database server, in this case DuckDB, will run inside the same process. And yeah, you can run queries there and the threads running and all the stuff that normally the database server does is happening in the process you're in.

And it has one crucial advantage. And that is that your database runs where the data is. So say you have data sitting in Python or you have data sitting in your client application. If you run the database server inside the same process, you have a giant advantage in terms of data interchange. So for example, very often we can actually do query processing without actually copying the data into the database because sitting there in memory, it's just bytes. DuckDB knows how to read a wide range of different data formats from memory. And then we can just run queries on that directly. And it's especially relevant for analytics use cases where you tend to transfer larger chunks of data to your application. For example, if you want to train a model or you want to do some complex visualization. This is something we found that is less relevant in the transactional use cases where typically you don't push millions of records to the client. But in the OLAP cases, that really happens. And yeah, traditional systems are not very good at that. We wrote a paper about that. I think that's the main difference.

Gotcha. So can you give me a description of the database architecture, like the backing storage system, and then we can go into query engine and so on?

Okay, happy to. So SQL systems have this interesting thing that the interface is pretty clear, right? You put in a SQL query and you expect some sort of result. It's pretty straightforward. So it simplifies your system design. And maybe we can walk through it like a query would walk through, right? So we start with the parser, obviously. And what we have done there, we're using a project that somebody has made that wrapped the Postgres parser in a separate library without the rest of Postgres. And we've heavily patched that thing. Basically, we run a derivative of the Postgres parser for transforming the SQL text into a parse tree. And that has one big advantage: we are quite compatible with Postgres query syntax. So that helps because lots of people know that particular SQL syntax. So then we have a parse tree. We take that, we transform it to a logical query plan, that's our internal representation. We have an optimizer that optimizes the query plan, producing another version of the logical query plan. And then that goes into a pipeline-based vectorized execution engine, which is something that vectorized engines are known but not incredibly well known. This is a technology that is particularly useful for OLAP kind of queries.

And then on the storage side, we have full MVCC transactions on top of a storage format that is a single file storage format. The entire database lives in a single file. And there are just fixed size blocks that contain the table data in a vectorized columnar data representation where you have columnar data but you chunk these columns up into row groups to localize rows a little bit. What is important though is that DuckDB is different from many other systems in that it doesn't have to actually own the data to run queries on top. Basically if the data is compatible with what we understand, we don't have to actually import anything to run queries. For example, we can run SQL directly on Pandas data frames, Arrow data frames, streams, all sorts of stuff. And this is because we think it's cool. And it's also because there has been, from our experience talking to people, a bit of resistance to handing over your data to some other system, because people have made the experience that once they do, they never get it out again. So we say, maybe we can just run queries on the data wherever it lives. But this is kind of the structure. I'm happy to go into detail on any of these things.

Let's go through the step-by-step read path of an OLAP query in DuckDB. Okay. So when you have a select query, that would be the way to read stuff in SQL. And that becomes a logical operator for the scan. And as I mentioned, the scan can run from multiple sources. We'll just assume that we read from the internal storage for now. It's actually interesting, because we run on so many different data formats, we actually treat all the different scans the same way, including our own scan. So in DuckDB, all the scan operators are kind of pluggable and you can have your own, you can bring your own scan operator if you want, and some people do.

But let's just assume we use our own storage. So there is this file format with fixed size blocks to store the tables, and basically in those blocks there's table data in these row groups that contain columns for the individual columns. And the scan operator will start reading vectors of data from these blocks. That means that traditional systems will read the data row by row. We also will not read an entire column of the data like some other systems. We read a row group, which is in our case the default is about a thousand rows, but we will read these column chunks of a thousand values for the part of the table that we need for the query. And then we have something we call a data chunk and that becomes the intermediate input for the operator that sits on top of the scan. That can be really arbitrary, depends on what the query wants to do. It could be a filter, could be an aggregation, could be a join, could be a sort.

And basically the important thing is that the basic unit of data that flows through the DuckDB execution engine is these vectors of, by default, 1,024 values. There's a reason for this. The reason is that the vectorized engine is called a vectorized engine because it uses these vectors of data. It does this because it doesn't actually compile queries like some other engines do, but it still interprets the query plan. Because it interprets the query plan, there's a slight overhead in the interpretation, and to amortize this overhead, we just look at a bunch of values at the same time. Another big advantage of using these vectors is that if you have an expression, for example A plus B plus C in your projection, because we are always operating on these fairly small vectors of a thousand values or so, these intermediates of the computation stay in the CPU cache. And that is a very good thing for performance. That's the design rationale behind the vectorized engine. And basically the entire engine operates on these streams of vectors. Every single operator in the engine deals with this format.

Of course, there are blocking operators that have to materialize the input. For example, in a join you have to materialize one side, or if you want to sort data you have to materialize. But apart from those, the engine streams these sets of vectors through the query plan. It does this automatically in parallel. So it's pretty cool. We take the query and we have this pipeline parallelism engine that automatically figures out how to run the query in parallel, and we will use all the threads that are available. And once we have pushed this stream of vectors through the entire query, you get it as a user. The client API will deliver those vectors to you if you want, and because you're in the same process space, you can basically just directly interpret these and that's all fairly efficient.

The main metric for OLAP engines is something we call cycles per value. Basically that says how many CPU cycles are we spending on processing a single value, a single row, whatever you like of the input. And that's really the biggest target metric that we're trying to optimize for because it's super relevant and there is easily a thousand X difference between SQL systems in this particular metric.

So it's worth talking about other alternative query methods. If I was to use Snowflake, for example, for issuing a large scale query, how would that execution path differ from DuckDB?

That's interesting because one of the co-founders of Snowflake comes from the same lab at CWI where DuckDB comes from. So Snowflake, as far as I'm aware, also uses a vectorized execution engine. So it would actually be fairly similar in the big lines, the general scheme of things. I think what you have in the Snowflake case is the distributed execution path, which DuckDB is focusing on the single node. That's a pretty huge architectural difference. And I think this is interesting because people for the last 20 or so years have been working really hard on building distributed execution engines. And in my opinion, people have been neglecting the single node execution performance a lot. As a matter of fact, they've been neglecting it so much that single node execution has actually become slower.

I think people have also forgotten how much you can actually achieve on single node. I have this MacBook here and there's maybe, have you heard of the New York City taxi data set? Sure. Yeah. So this is usually used a lot in benchmarks for big data systems. You can basically run queries on that thing in its entirety on my MacBook here with DuckDB and not use a distributed system. And I feel like this option has faded into obscurity for some reason, even though it is for the overwhelming majority of data sets the much better option than going distributed.

I think if you wonder about the difference between how DuckDB does queries versus what Snowflake does, something like Snowflake or Presto will spend a huge amount of time on the coordination of the distributed query execution, including partitioning and shuffling stuff around, coordinating execution, pulling results back in. And they're spending a lot of time on that. And I think in many cases that will actually dominate the execution time. So if you're not doing that because you have a giant computer with a terabyte of main memory or something like that, you can gain a lot of performance.

And when you say performance in this context, do you mean speed or cost savings or what?

I think it's both. So I think what has been recognized at this point is that the cloud SQL systems are not really able to deliver interactive performance. If you have something sitting in Presto and you're clicking a button, if whatever the button needs to do requires a big query running in Presto, you will not be able to do this interactively really. This has been promised a couple of times. I don't think it really has happened. Same with Spark. They talk a lot about how they use memory, but I haven't seen a lot of Spark jobs completing under a second. So you can get a lot of quick performance from using something like DuckDB, which just doesn't have to do this coordination game with hundreds of nodes. And of course, if you have higher single node efficiency, you will also spend less on your cloud provider.

I did this one experiment. We ran a query on this taxi dataset, used DuckDB. And then we looked how big does the Spark cluster have to be to match this performance. Take one node running DuckDB with this particular query. Now we have to see how many of those nodes do we need to beat this performance with Spark. And I think the answer was 32 or something like that. And that's a 32X increase in cost. You have to pay Amazon 30 times the money for that. So that's pretty significant.

And in general, there's this misconception that everybody is sitting on these petabytes, but while some people do, I would think studies show that the majority of data sets people actually look at easily fits on something like a laptop. And it's more like there has been a shortage of tools that can actually take advantage of something like a laptop. Because what can you really do if you want to do something like OLAP or general data wrangling of relational data sets?

One question I have about DuckDB is around data management in terms of memory usage. So if I'm performing a large query or a join, I'm going to be pulling a lot of data into memory and performing operations on top of that. And depending on what programming language I'm executing in, there might be different data structures pulled into memory. And I know you use Apache Arrow to have data integration between Python and other data formats. Can you talk about memory management in more detail and how Apache Arrow fits into that?

Certainly. So we use Arrow as an integration platform. We only really use it at the boundaries. We can read Arrow data as if it were a table in DuckDB. And we can export query results back to Arrow. Both can work with zero copy. That's not so relevant for the internal state of the query engine because we're not using Arrow there. We use a similar format but it's not the same.

Let's say we do a join. For memory management in a join, obviously the join is one of these blocking operators. If you run a hash join, you have to materialize, build a hash table on ideally the smaller side of your join. And that needs to sit somewhere. Traditional in-memory engines would just put that hash table in memory and build it and then scan it in the probe phase. That works. And that's what DuckDB does by default. If you have enough memory, we will just use memory. We put a hash table that is a custom hash table in binary encoding. It's kind of funny because hash tables are one of these places where a columnar engine is forced to encode data in a row major format again, because it's just the way you refer to it in a hash table. So you have to give up on your columnar dreams in hash tables, also in aggregate hash tables, sorting, and so on. Kind of funny.

But then this hash table sits in memory. Now what happens if this becomes too big? This is super interesting because it's something we're spending a lot of time on at the moment, working on this thing that we call the "never give up, never surrender" project. We want to make sure that even if you're running out of memory, we still are able to complete the query successfully. So this is where we actively work. And the general philosophy we're following is to gracefully degrade as we start using disk. This is different from what traditionally happens because in traditional systems, they will run happily in memory until they realize this doesn't fit in memory anymore. And then they switch to an entirely different algorithm, for example go from a hash join to a sort merge join. We've decided we don't want to have these hard cuts in operator implementation because it creates these unexpected performance cliffs. It's something people really dislike about relational systems, that the query is the same but you add one more row of data and suddenly an entirely different algorithm is executed and they have no handle on this.

So what we like to achieve is this graceful degradation of performance as we run out of memory by using the disk to basically write stuff that we can do without for the moment while still achieving progress. The way we can do this, for example in the hash aggregate, is by partitioning things like hash tables by hash values and then dumping stuff to disk that you're currently not needing to match hash values. So there is some active research in our group currently on making this out of memory situation survivable, which I think is kind of new for in-memory OLAP engines because traditionally they have just assumed that the working set will somehow fit in memory. And we are working on changing that. It's also because DuckDB is aimed to run kind of anywhere, and by anywhere we also mean devices like phones that have a different memory setup than a high-end server.

What is the typical deployment model for DuckDB? What are the memory and storage requirements?

There's really not a lot of requirements. It works really well on Raspberry Pis if you want. In fact, we had an experiment. We ran DuckDB on a Raspberry Pi and we ran SQLite on a $5,000 server running the same benchmark and DuckDB ended up being faster on the Raspberry Pi. Of course, that was an unfair comparison because we were using an OLAP benchmark which SQLite is not built for, but it was interesting to see. Our general goal is really that it runs wherever. It should run from anything with a single core and 500 megabytes of RAM up to a server that has 200 cores and a terabyte of RAM. And in fact, we have one of those and it works perfectly well there as well.

I think in terms of typical deployment, we see a lot of MacBooks because DuckDB is used a lot by data scientists working from R and Python. It's just people on their laptops basically. And it's quite impressive what you can achieve with that kind of hardware if actually using it. And it's kind of interesting because it's really quite rare in data science tools to parallelize things. It's only now starting to become a thing. And by auto-parallelizing queries, we can use that hardware to a much larger degree. There have been some issues reported where people said my computer suddenly becomes much hotter than it has ever been. But that's something we have to live with.

You wrote a blog post about using WebAssembly together with DuckDB. I guess this wasn't written by you specifically, but by your team. And given that this is what I would consider generally a backend technology, an analytic database, it's interesting to see a use case of browser-based analytical query engines. Why would it make sense to use an analytical database in the browser? And why would WebAssembly be a good application for that?

This is one of these things that have blown my mind completely. This was not initiated by us directly, but by Andre Kohn, who is from the Technical University of Munich. And we work together now. What he's done is to basically take DuckDB and cross-compile to WebAssembly. The reason he can do that is because DuckDB doesn't have a lot of dependencies. It's lightweight in terms of that. And so it was possible to cross-compile to WebAssembly and then run the entirety of DuckDB in the browser. In fact, we have a demo on our website where you can just run DuckDB in the browser and get a shell and you can type queries there.

Why would you want to do this? I think in general, we see a lot of data being pushed into the browser and it has to do with the backend latency. If you're trying to build an interactive visualization and you want to have something like a slider, something pretty trivial, if every slider move requires a call to a backend and possibly a Snowflake query or a Databricks query, that's not going to be happy eyeballs. Just because the backend latencies are too large. So it does make total sense to push a dataset into the browser directly and basically recompute your visualization input there instantly. It's really funny if you ever see this in a demo, it's so instantaneous that you wouldn't believe it.

I think the reason why people push OLAP engines into browsers is because of latency and because of these vastly underappreciated capabilities that you have in browsers now with WebAssembly technology that allow you to run something like an analytical SQL engine entirely inside the browser. We also see that people take their backend database system and then export data that is relevant to a particular visualization as Parquet files, which DuckDB can read natively, also from the browser by the way. And then they export this Parquet file into the browser and have the visualization generate SQL queries and run them on this subset of the data. So that's the reason why people find this in the browser useful. We're also working together with a company called Stoic that is basically running a data analysis workbench as a progressive web app, which also uses DuckDB.

And can you speak to the functionality of WebAssembly in allowing that browser-based OLAP querying? Yeah, I mean, you could of course write a SQL engine in JavaScript. Some people have tried, but I'm not entirely sure whether this would really be that fast. In WebAssembly, you can leverage existing implementations in C++. Since DuckDB is implemented in C++, you get the WebAssembly version as a side product if you want. Because you can, just in air quotes because there's some work required, take an existing engine and cross-compile it to WebAssembly and then run it in browsers.

I think the surprising capability is that this is something that has been built into browsers in the first place. My 90s kid sort of brain is still slightly warped by seeing what people push into browsers. But it's really cool that you can cross-compile projects like DuckDB and just run them in the browser. We've done some benchmarks to see what the performance hit is compared to the natively compiled C++ version. And it wasn't so crazy. It was something like 30% slower, which really isn't so bad if you think about it, considering what machinery is running to make this work.

I think it's just super exciting. We were super excited to see this work because it opens an entirely new world where traditional OLAP engines would never have been able to set foot into. Traditional engines are far too heavyweight to be considered for cross-compilation games. You have hundreds of dependencies. I don't know if you ever tried to install something like ClickHouse, but I did. You're looking at hundreds of dependencies including like three compilers or something like that. And that's just not going to happen. You're not going to put that in the browser. But that's actually one of the advantages of vectorized execution engines compared to the more standard just-in-time compilation engines, that they can be built much more lightweight because you don't need the entire compiler infrastructure. It's still an interpreted engine. It's just more efficient than your normal interpreted engines.

Could you dive a little deeper into comparing interpreted engines versus compiled engines?

Yeah, there's an interesting paper for people that are interested. I think the title is something like "What you always wanted to know about vectorized versus JIT, but never dared to ask," something like this. So I mentioned before that the metric we're looking at is cycles per value. We want to build an engine that is as efficient as possible with regards to CPU cycles spent per value. Now, the reason traditional systems like Postgres are slow is that for every row of data, they have a lot of levels of indirection, function pointers, type switching for every single row that makes it basically pretty slow. That gives them a pretty large cycles per value.

How can we get rid of these? Well one way is to JIT it. Instead of interpreting the query plan on a row by row basis, you turn the query plan into an executable program by JIT compiling the query. That's what engines like, this has been done all the way back in System R, and most recently done in OLAP engines by a system called Hyper, which is now part of Tableau, also from the University of Munich. The thing is, with JIT engines there's quite high complexity in building them because you're generating code and if something goes wrong, you have to debug generated code, which is pretty gnarly. You need a lot of compiler infrastructure to turn a query plan into an executable because this needs to be done on an assembly level, or an LLVM assembly level.

A vectorized engine tries to amortize the overhead of interpretation by making every individual operator able to deal with multiple values at the same time. So instead of operating on single values, DuckDB's addition operator will take two times 1,024 values and produce 1,024 new values as a result. That way we get the cycles per value down because we only have to do the switching on types and which operator to run once for every 1,024 values at a time. And that really drives down the cycles per value. That is what makes it go fast. And I think that's a big advantage of vectorized engines.

There's an ongoing discussion in the academic world on which one is better. My take is they're generally comparable but they have advantages and disadvantages. And one of the advantages of vectorized engines is that they can be built much more lightweight. I think one of the reasons DuckDB was able to stay fairly nimble despite supporting a huge chunk of the SQL standard.

What's your strategy around monetizing DuckDB?

So DuckDB was built first at the CWI. The CWI is the Dutch National Research Institute for Computer Science and Mathematics. It's where Edsger Dijkstra was a long time ago. Guido van Rossum invented Python not so long ago. And in that research lab, there's a database group and I'm part of it. We built DuckDB in the first place there because we thought it was the right thing to do. We started building it about four years ago. We got some grants to work on it. But it gets tricky to build a software development team inside a research institute because a research institute is not necessarily meant to do this. They're meant to write papers and educate PhD students. Building a software development team inside a research institute has its limits.

So last year, we spun out DuckDB Labs from CWI, which is a new company now working on pushing DuckDB further. I'm spending most of my time in DuckDB Labs. I'm also the co-founder and CEO. How do we monetize? Well, we haven't taken VC money. So we're not sitting on a giant pile of cash that we're slowly shoveling into a big fire. But we are actually using this very traditional method of having customers that pay us for our services around DuckDB. DuckDB is free and open source and most of our users don't pay us. The vast majority don't pay anything. And we're very happy about that. But there are organizations that want to use DuckDB as a building block in their infrastructure. They want to build a large data pipeline thing around DuckDB, for example. And then maybe they have custom requests or they want us to prioritize work on specific issues. So the way DuckDB Labs makes money is by contracting with companies that need custom work around DuckDB or that need specific things to be prioritized. And we charge them for that.

At the same time, we also collect donations in a separate entity called the DuckDB Foundation. Basically if you would like to support the development of DuckDB, you can become a supporting member of this foundation and we will use that money to pay for the maintenance. So that's the model we have.

And what are the types of users that you're seeing adopt DuckDB?

There's two camps, I would say. There's the data scientists that are just using DuckDB on their laptop. And there's quite a lot of them, but they just tend to be happy with the way the thing is. And then there's the other camp, which are other software companies that are building larger infrastructure that uses DuckDB as a building block, that uses DuckDB as the database as a library. Or maybe it's quite common that they only use some components. For example, we have some customers that are not using our execution engine but are only taking the plans as they're generated and doing something else with them. Or there's another customer that doesn't want to do that but wants to use the execution engine but not the SQL front end. So they generate plans themselves and hand them to us for execution. There's all sorts of combinations of use. And those are the more complex use cases. And those are the ones we work with to add hooks.

In general, so far it's been working out really great to take these requests for changes, think about ways in which we can make something generic that will benefit multiple people, and build that into DuckDB. Thereby pushing the platform with these requests from individuals. I think one thing that's different about DuckDB is that we are not an Apache project or anything, and since we started as a new take on OLAP, it was a bit opinionated at the beginning. But we've kept this opinionated design in that we still think very hard about whether a particular feature makes sense for the overall project or not. And I think that has so far worked out pretty well to keep the thing clean and small and efficient.

Well, let's close off with the toughest engineering problem that you've had to solve when building DuckDB.

Here I can praise my co-founder, Mark Raasveldt, who is the CTO of DuckDB Labs and was my PhD student a long time ago. And there is something called subquery flattening. Are you aware of this problem? No. I thought so. It's obscure. But in SQL, you can have a subquery, a query inside the query. And you can have a correlated subquery where you have, for example, a comparison, like a filter inside a subquery that refers to things from the encapsulating query. That's called a correlated subquery. And the problem with correlated subqueries is that there is a naive way of evaluating them and a correct way of evaluating them. The problem is that in the naive way, we have to conceptually re-evaluate the subquery for every row of the encapsulating relation. So for every tuple in the outer query, we have to rerun the subquery with the correlation variables filled from the outer relation. This is what Postgres does. Now this works for Postgres because they're an OLTP system. If you try to do this on a table that has a billion entries, you're re-evaluating the subquery a billion times. This is not going to happen.

So in order to fix this, you have to do something called subquery flattening where you cleverly rewrite the query into a join with new and exciting join types to basically eliminate the correlated subquery and replace it with a strange join. And Mark managed to implement this in something like two months. And then basically turned up and was like, it's done. And I was absolutely blown away that this was possible because I had looked at it and was completely overwhelmed by the involved complexity. It is something that is really badly described in the literature. Nobody really talks about it. There was one paper from the University of Munich that described the path towards success, but I was absolutely impressed when that particular problem was solved. I think that's the overall toughest problem.

I'm trying to think of my personal hardest problem in DuckDB. And there is one. I think reading Parquet files may have been a tough nut. You know Parquet files, right? Yeah, certainly. It's like a columnar table format for files on disk. It can be recursive, but it's usually tables. And this Parquet format is everywhere. It's the standard table format. And the thing is everybody just uses an off-the-shelf reader for this. Everybody tends to use the same reader, which is Parquet-MR or something, comes from MapReduce way back when. And for various reasons, and also because we hate dependencies, we decided to write our own. And this is one of these problems where you start digging into a file format where there is a reader for it, but the specification is kind of whatever that reader does. Or whatever the writer does. It's a file format where there's not really great documentation, but you have to dig through the various implementations to understand what on earth is going on. And I spent an ungodly amount of time understanding, for example, the encoding of nested tables in Parquet. But of course the great result is that I think DuckDB now has a very competitive Parquet reader that has zero dependencies. So something good came out of that.

Awesome. Well, listen, thank you so much for coming on the show and talking about DuckDB. Exciting to see another entry into the world of OLAP querying. Thank you so much for having me. It's been a pleasure.
